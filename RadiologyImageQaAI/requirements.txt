fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-multipart>=0.0.9,<1.0

# Imaging / DICOM pipeline (required for /api/v1/qc/dicom)
pydicom>=2.4,<4.0
dicom2nifti>=2.4,<3.0
nibabel>=5.2,<6.0
numpy>=1.26,<3.0
SimpleITK>=2.3,<3.0

# Segmentation (optional at runtime; endpoint fails gracefully if not installed).
# Disabled for the demo: TotalSegmentator pulls torch + nnU-Net + models (multi-GB).
# TotalSegmentator>=2.5,<3.0

# Optional OpenAI report phrasing (never used for detection)
openai>=1.30,<2.0

# Dev / tests
pytest>=8.0,<9.0

# The stack supplies these from the ROOT .env via docker-compose (x-llm-env).
# Only needed when running this service standalone. Never commit .env.

OPENAI_API_KEY=sk-your-api-key-here
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-4o

TEMPERATURE=0.2
MAX_RETRIES=2
PORT=8028

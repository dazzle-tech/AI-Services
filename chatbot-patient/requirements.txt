# Pins mirror chatbot-clinician/requirements.txt so both bots resolve to the same
# versions. Python 3.11.
#
# `medai-core` (shared/medai-core) is NOT listed here: the Dockerfile installs it
# from the repo before these requirements. Running a bot outside Docker needs:
#     pip install -e shared/medai-core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Agent graph
langgraph>=0.2.0

# LLM + tool calls
openai>=1.0.0
requests>=2.31.0

# Session store (falls back to in-process if unavailable)
redis>=5.0.0

# PostgreSQL (registered_users phone → patient lookup when DB_TYPE=postgresql)
psycopg2-binary>=2.9.0

python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
httpx>=0.27.0

# FastAPI and Server
fastapi==0.109.0
uvicorn==0.27.0
pydantic>=2.5.3

# OpenAI
openai>=1.30.0
httpx>=0.27.0
httpcore>=1.0.0

# Environment variables
python-dotenv==1.0.0

# Vector store + embeddings (RAG template selection)
chromadb==0.4.22
# CPU-only torch (avoids ~2GB of nvidia CUDA packages on a CPU-only demo build).
numpy>=1.26,<2
torch==2.2.2
sentence-transformers==2.3.1

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5

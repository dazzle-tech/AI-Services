version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: medical-guideline-validation-service
    ports:
      - "8008:8008"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8008/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
